<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Happy Birthday Koyal 🎂</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      color: #fff;
      text-align: center;
      overflow: hidden;
      position: relative;
    }

    /* Card styling */
    .card {
      background: rgba(255, 255, 255, 0.15);
      padding: 30px;
      border-radius: 25px;
      backdrop-filter: blur(10px);
      max-width: 350px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.2);
      z-index: 2;
      position: relative;
    }

    .photo {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid #fff;
      margin-bottom: 15px;
    }

    h1 {
      font-size: 28px;
      margin-bottom: 5px;
    }

    h2 {
      font-size: 18px;
      font-weight: normal;
      margin-bottom: 15px;
      opacity: 0.9;
    }

    p {
      font-size: 16px;
      line-height: 1.6;
    }

    .emoji {
      font-size: 32px;
      margin: 10px 0;
    }

    .btn {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 25px;
      background: #fff;
      color: #ff5f7e;
      border-radius: 30px;
      text-decoration: none;
      font-weight: bold;
      transition: 0.3s;
    }

    .btn:hover {
      background: #ff5f7e;
      color: #fff;
    }

    footer {
      margin-top: 18px;
      font-size: 13px;
      opacity: 0.85;
    }

    /* Heart styling */
    .heart {
      position: absolute;
      width: 20px;
      height: 20px;
      transform: rotate(-45deg);
      opacity: 0.8;
    }

    .heart::before,
    .heart::after {
      content: '';
      position: absolute;
      width: 100%;
      height: 100%;
      background: inherit;
      border-radius: 50%;
    }

    .heart::before {
      top: -50%;
      left: 0;
    }

    .heart::after {
      left: 50%;
      top: 0;
    }

    @keyframes float {
      0% {
        transform: translateY(0) translateX(0) rotate(0deg) scale(1);
        opacity: 0.8;
      }
      25% {
        transform: translateY(-100px) translateX(-20px) rotate(45deg) scale(1.1);
      }
      50% {
        transform: translateY(-200px) translateX(20px) rotate(90deg) scale(1.2);
        opacity: 1;
      }
      75% {
        transform: translateY(-300px) translateX(-15px) rotate(135deg) scale(1.1);
      }
      100% {
        transform: translateY(-500px) translateX(10px) rotate(180deg) scale(0.8);
        opacity: 0;
      }
    }

    /* QR code styling */
    .qr-container {
      margin-top: 20px;
      position: relative;
      display: inline-block;
      z-index: 3;
      animation: pulse 2s infinite;
    }

    .qr-container img {
      width: 120px;
      height: 120px;
      border-radius: 20%;
      border: 5px solid #fff;
      box-shadow: 0 0 20px rgba(255,255,255,0.8);
    }

    @keyframes pulse {
      0% {
        transform: scale(1);
        box-shadow: 0 0 20px rgba(255,255,255,0.8);
      }
      50% {
        transform: scale(1.1);
        box-shadow: 0 0 40px rgba(255,255,255,1);
      }
      100% {
        transform: scale(1);
        box-shadow: 0 0 20px rgba(255,255,255,0.8);
      }
    }
  </style>
</head>
<body>

  <!-- Floating hearts -->
  <script>
    const totalHearts = 50;
    for(let i=0; i<totalHearts; i++){
      const heart = document.createElement('div');
      heart.classList.add('heart');

      // Random size
      const size = Math.random() * 20 + 10;
      heart.style.width = `${size}px`;
      heart.style.height = `${size}px`;

      // Random position
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.bottom = -50 + 'px';

      // Random color (pink-red tones)
      const r = 255;
      const g = Math.floor(Math.random()*150);
      const b = Math.floor(Math.random()*150);
      heart.style.backgroundColor = `rgb(${r},${g},${b})`;

      // Random animation duration and delay
      const duration = Math.random() * 5 + 5; 
      const delay = Math.random() * 5;
      heart.style.animation = `float ${duration}s linear ${delay}s infinite`;

      document.body.appendChild(heart);
    }
  </script>

  <div class="card">
    <img src="koyal.jpg" alt="Koyal" class="photo">

    <h1>Happy Birthday 🎂</h1>
    <h2>Koyal 💖</h2>

    <div class="emoji">👀✨</div>

    <p>
      Dear <b>Honey</b> 🍯,<br>
      You are not just my best friend,  
      you are my safe place, my smile,  
      and my forever vibe 🌸💫  
      May your day be as beautiful as you 💕
    </p>

    <div class="emoji">🎉🎁💖</div>

    <!-- QR code -->
    <div class="qr-container">
      <a href="https://www.instagram.com/koyal_0o0?igsh=YXh5czR1cWVjdGdh" target="_blank">
        <img src="https://api.qrserver.com/v1/create-qr-code/?size=120x120&data=https://www.instagram.com/koyal_0o0" alt="QR Code">
      </a>
    </div>

    <footer>
      — From your bestie 🤍
    </footer>
  </div>

</body>
</html>
